// crates/gui/ui/components/section_header.slint
// All-caps small label used as a section divider inside a page.

import { Tokens } from "../tokens.slint";

export component SectionHeader inherits Text {
    color: Tokens.on-surface-dim;
    font-size: Tokens.font-meta;
    font-weight: Tokens.weight-strong;
}
