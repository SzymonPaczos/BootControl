#!/bin/sh
#
# Include the failsafe entry generated by bootcontrold in grub.cfg.

set -eu

failsafe_cfg="${BOOTCONTROL_FAILSAFE_CFG:-/etc/bootcontrol/failsafe.cfg}"

if [ -r "$failsafe_cfg" ]; then
    cat "$failsafe_cfg"
fi
